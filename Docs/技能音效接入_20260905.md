# 技能与法师蓄力音效

本次采用已确认的四条音效，重复写出的 ShadowStep_B 按同一选择处理。

| 行为 | 音效 | 触发时间 |
| --- | --- | --- |
| 疾步之靴、猫之灵巧 | SpeedBuff_A | 技能成功生效 |
| 冰封 | FrostBind_B | 点选有效敌人并施加成功，不在进入选目标状态时播放 |
| 踏影 | ShadowStep_B | 瞬移成功 |
| 法师蓄力 | MagicCharge_A | 现有法师准备攻击阶段 |

惊惧斩和幻影仍使用原来的 SkillUse_01，等待后续挑选。每次技能生效只选择对应的一条技能音，不额外叠加旧通用技能音。卡片打出、角色语音、失败操作提示、法术释放及命中音保留。

资源目录：Assets/Resources/Audio/Skills/SelectedModel。
来源：Downloads/kait-sfx-model-audition-20260904/skills_round1。
四条 WAV 直接复制，SHA256 与试听文件相同；没有新增裁剪、淡出、增益、归一化或变速。踏影保留试听版 0.30 秒，其余均为 1 秒。Unity 使用 48 kHz 立体声 PCM，关闭导入归一化。原有运行时混音倍率保留，法师蓄力沿用原有轻微随机音高，因此游戏中的听感受混音影响。

改动前 GameAudio.cs、KaitGame.cs 和项目设置备份在 Backups/skill-audio-before-20260905。旧音效资源未删除。此次只更新 Windows，不编译 Android。

验证：358/358 EditMode 测试通过，报告为 Logs/selected-skill-tests.xml。新增 12 项检查覆盖四条素材的导入、七种技能分支选音，以及三个施放入口传递技能类型和法师蓄力引用。音频文件均与试听版一致；这些文件与逻辑检查不代替实际游戏内听辨。

Windows 构建本次未成功：测试通过后，同一项目的边界特效预览代码新增了对 run.katePos 的直接赋值，但该属性只有 private set，导致 KaitGame.cs 第 3486 行 CS0272。构建日志为 Logs/selected-skill-build.log，退出码 1。本轮未修改或回退这段并行新增的特效代码；已恢复失败构建引起的 Standalone 编译符号临时变更。

四条音效及技能分流已写入 Unity 项目，但尚未进入 Windows 成品。Build/kait_Data/Managed/Assembly-CSharp.dll 仍为 2026-09-05 09:30 版本。应先解决上述编译错误，再重新测试和打包。

后续更新（2026-09-05 11:11）：上述编译阻塞已在当前项目中解决，四条音效已随刀波、幻影和身体受击音效进入 Windows 成品。378 项测试通过，最终构建日志为 Logs/speed-b-build.log；七条资源和代码引用已逐一检查。详见《刀波幻影受击音效接入_20260905.md》。前段保留的是首次尝试的历史记录。
