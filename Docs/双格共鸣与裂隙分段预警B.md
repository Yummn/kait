# 双格共鸣与裂隙分段预警 B

双格共鸣使用同步的短暂金色边线标出副棋盘合成来源与主棋盘实际刷新目标，持续约 0.46 秒。以本回合实际保留的刷新请求为准，使用重定向后的目标；被取消的刷新不创建对应高亮，同格请求去重。连杀等待转向分支也会显示配对反馈。

裂隙 B 改为严格俯视的四段焦橙色警示，按格子实时尺寸构建，距离四条边固定内缩，中心保持空白。人物站在裂隙上时仍能看到边缘，保留原有头顶危险标记。预警在刷新请求存在时脉动，取消、完成刷新或重新开始后清除。原有整格缩放改由边缘预警承担。

为了精确贴合四边，分段轮廓使用 UI 几何绘制，沿用 B 的分段、内向尖角及焦橙配色，不将菱形透视原图拉伸到方格。信号跟随目标格的实际边界，不参与点击或遮罩。

验证：Windows 构建通过，人物占格、目标重定向、取消刷新、常规游戏四个运行场景通过。检查了配对数量、警示顶点均位于格内、信号清理，并查看实机截图 `VFXScreenshots/linkb-final.png`。该检查场景直接建立刷新请求，副棋盘高亮格为空，仅用于验证几何和配对；正常玩法从实际合成事件触发。

构建日志：`Logs/linked-cells-b-build.log`。
检查脚本：`Tools/verify_linked_cells_b.ps1`。
备份：`C:/Users/yummn/Downloads/kait-backups/linked-cells-b-before-20260905-153452`。

卡片、胜负收尾继续暂缓。未构建 Android、未上传 GitHub。
