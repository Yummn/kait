# 第六组预警 A / C / B 接入

用户选定：连杀等待时钟 A、即将受伤红边 C、盾骑士整排攻击预警 B。

## 表现

- 时钟：八帧、每帧150ms循环；左下96×96。保持Kait连杀等待1.2秒后淡入，逐渐加深灰边；继续行动后退去。不扩大到Yummn新规则时停。
- 红边：八帧双角交替，保持原危险判定。首轮运行看到整屏拉伸压住棋盘，改为固定四角九宫格、中心不绘制，透明度最高0.68。不改素材位图。
- 盾骑士：Yummn整排攻击范围每格一张B帧，按真实方向旋转，位于人物下方；不整排拉伸。死亡/意图解除后隐藏，Kait模式原预警不变。
- 三种图像都不接收点击，不等待动画播完，不改音效或回合规则。

资源从 `VFXPreviews/YummnFrames-Round6-20260910` 复制到 `Assets/Resources/KaitVisuals/Yummn/Frames/ClockA.png`、`DangerC.png`、`BossB.png`。三个文件与各自选稿SHA256逐一一致，原选稿保留。

## 验证和包

- `Logs/warnings-acb-tests-final.xml`：837/837通过，其中6项新增，包含八帧循环、透明隐藏、非阻塞、角色/存活/冻结/攻击范围条件和方向。
- `Logs/warnings-acb-build-final.log`：Windows成功；`Build/kait_Data/Managed/Assembly-CSharp.dll` 为2026-09-10 21:59:19，521216字节。
- `Logs/warnings-acb-runtime.log`：WARNINGS_QA_COMPLETE。实际运行生成四方向预警及clock/danger/both/clear截图，最终查看了红边贴边和纵向预警画面。
- 最终截图在 `Logs/warnings-acb.*.png`。时钟/红边截图通过测试入口强制显示指定状态，不冒充自然触发时刻；四方向预警使用实际显示组件和测试Boss意图。
- 运行前24项玩家设置/存档已按名称、类型、值恢复并校验。退出仍有原有ComputeBuffer释放提醒，未发现此次QA异常。
- 本次没有构建安卓，没有上传GitHub。18:44的v0.8.2 APK不包含这三个后续特效。

v0.8.2逻辑残影的独立碎裂/穿空音效仍待审批，与本组六组预警无关。
