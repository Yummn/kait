# 惊惧斩蓄势声

采用试听版 DreadCharge_B，1 秒、48 kHz 双声道 PCM，文件原样复制，不改音量、不做淡出或峰值处理。

惊惧斩成功使用时由现有 PlaySkillUse(skill) 选择此音效，替换旧 SkillUse_01。刀波实际释放仍播放 DreadSlash_B，其他技能、语音及播放时机保持不变。

资源：Assets/Resources/Audio/Skills/SelectedModel/DreadCharge_B.wav。

修改前的音频代码、测试及项目设置保存在 Backups/dreadcharge-before-20260905。此次更新 Windows 版，不重编安卓或推送 GitHub。

验证：385 项 EditMode 测试通过（Logs/dreadcharge-tests.xml）。Windows 构建成功，Unity 返回码 0（Logs/dreadcharge-build.log）；已确认成品程序集引用 DreadCharge_B，资源包同时保留 DreadCharge_B 和 DreadSlash_B。
