# v0.9.16 锁盘静止

2048棋盘四个方向都无法移动或合并时，不再判负，也不再重置棋盘。

- 数字、墙体及格子状态保持原样。
- 后续方向操作不会让2048盘移动、合成或补入新数字。
- Kait、Yummn及敌方阶段继续按各自原有规则行动。
- Yummn等待时同样不会因为锁盘结束游戏。
- 原2048控制器不再弹出Game Over界面。
- 生命归零仍然失败，击败盾骑士Boss仍然胜利。

教程、结算文案和相关回归测试同步更新。规则以只取消锁盘终局为范围，其余战斗、补给、裂隙和敌方时序不变。
