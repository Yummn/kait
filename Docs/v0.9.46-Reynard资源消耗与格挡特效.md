# v0.9.46 Reynard 资源消耗与格挡特效

## 接入内容

- 法印消耗采用 C「星印碎散」。
- 狐尾消耗采用 C「尾焰献祭」；施法支付和狐尾抵伤均使用该序列。
- 镜衣结界格挡采用 A「镜月护壁」。格挡发生时新增独立 `MirrorWardBlock` 视觉事件，不再只静默取消伤害。
- 三套正式资源分别为 `SigilSpent.png`、`TailSpent.png`、`MirrorWardBlock.png`。

## 新审批组

- 分裂弹：翠绿晶体 / 珊瑚翼片 / 紫电双彗。
- 逐星弹：金黄五星 / 绯粉星针 / 霜白流星。
- 狐尾溢出：紫色能量潮 / 绯红多尾警示 / 青绿尾转法印。

候选特意使用不同主色、轮廓与运动语义；未经选择不接入。

## 验证

- 首次构建在仅供截图的QA代码中发现错误层级调用，已修正；没有把该失败构建作为交付。
- `Logs/reynard-vfx-cca-final-build.log` 最终构建成功，版本 0.9.46。
- `Logs/reynard-runtime.log` 完成 `HOME0926_SELECTION_QA_COMPLETE` 与 `REYNARD_QA_COMPLETE`。
- 已查看 `reynard-sigil-spent.png`、`reynard-tail-spent.png`、`reynard-mirror-ward-block.png`：三种轮廓可区分，格位正确，透明边缘正常，没有整格白块。
- 九套新候选均有透明通道，三张并排GIF均为8帧；未生成 Android、未上传 GitHub。
