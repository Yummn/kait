# Yummn 逐帧特效接入

本次按选稿结果替换四项：拳击 B、连击 B、震慑 B、格挡 A。Windows 版已更新，Android 不在本次范围内。

| 效果 | 使用文件 | 播放时间 | 位置 |
| --- | --- | --- | --- |
| 普通拳击 | PunchB.png | 0.32 秒 | 被击中敌人的身体处 |
| 疾风连击 | FlurryB.png | 0.48 秒 | 被击中敌人的身体处 |
| 震慑 | StunB.png | 0.64 秒 | 敌人头顶 |
| 防御/格挡 | BlockA.png | 0.40 秒 | 防御者朝向来袭的一侧 |

资源在 Assets/Resources/KaitVisuals/Yummn/Frames。每张八帧，四列两行，保留生成原图及透明通道，导入时不压缩、不生成 mipmap、不重采样尺寸。其他未审批效果仍保留原版。

播放由命中、震慑和格挡事件触发，不增加等待时间。连击的一套三段画面只生成一次，避免每拳都重叠完整特效。仍保持敌人上方、主角下方的特效层；震慑不随攻击方向旋转。缺图时不显示白色矩形。

## 检查结果

- 最新 EditMode 测试 733/733 通过，含八帧切换、透明图初始化、原始尺寸、导入配置与原有功能回归。
- Windows 构建成功。使用同项目音效任务的最新构建，已包含本次视觉修改与同步音效，不另行覆盖音效工作。
- 实际运行完成四组特效的截图检查：未出现白底、方块和越界大面积遮挡；特效到期清理；特效播放期间方向输入仍生效。
- Yummn 移动、气竭步行、出拳、盾骑士远距预警以及切回 Kait 的回归检查通过。
- 运行日志有一条退出阶段 ComputeBuffer 释放提醒，无异常与 QA 失败记录。
- 测试前导出设置与存档，测试后还原，前后文件校验一致。

测试：Logs/yummn-frames-tests.xml；运行：Logs/yummn-frames-runtime.log；截图：Logs/yummn-frames.png.selected-*.png。

本次源文件备份：Backups/yummn-frames-before-20260910-125250。只备份本次涉及的原文件与测试前设置，不是整项目压缩包。原版图片未删除。

## 后续选稿

第二组已制作水鞭、气拳、冬之吐息、火蛇，各 A/B/C 三版，共十二套八帧动画。位置：VFXPreviews/YummnFrames-Round2-20260910。内置图像生成工具按独立方案生成；网页和视频为实际截图上的合成预览，不是游戏录像。选定后再接入。
