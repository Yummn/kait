# 环境轻微浮动

树冠增加两个低频波叠加的轻微摆动，并缓慢上下、左右浮动。1920×1080参考画面中，横向偏移不超过1.56像素，纵向不超过1.17像素，旋转不超过0.813度。

花草错开相位、使用较小摆动，旋转轴放在底部，避免根部滑动。树干、草坪、地砖和棋盘不动。

使用绝对时间计算偏移，不会随帧率改变速度或累积漂移。沿用原有每秒30次的实时阴影投射，使阴影跟随姿态；不新增贴图或粒子。

检查日志：`Logs/garden-float-tests.xml`、`Logs/garden-float-build.log`。
本次更新Windows，不重新生成APK。
