# 冰墓：碎冰束缚 A

使用已选定的 A 八帧素材，原图不重新绘制。冰晶为浅蓝、象牙白大色块和深色边线，尺寸 120×120，脚底锚点位于原图每帧高度的 83%。

冰晶位于敌人上方、Kait 下方，从脚边长出，避免盖住脸和武器。原有冰墓技能、一次行动冻结和已选音效保持不变。

动画分三段：约 0.22 秒生成，冻结状态下保持，解除或目标死亡后约 0.26 秒碎裂消散。保持时间由冻结状态决定，挂机不会自行解除。移动输入不等待冰晶播完。重开时清除旧效果；解除后的碎片停在最后脚底位置，不追着尸体走。

相关实现：`KaitIceBinding.cs` 管理显示生命周期，`KaitCombatEffectGraphic.cs` 播放八帧素材。`KaitGame.cs` 按敌人 ID 创建并同步冰晶，不重复叠加同一个冻结状态。

检查入口：`KaitIceBindingTests` 与 `Tools/verify_ice_a_runtime.ps1`。运行预览覆盖保持、跟随、解除、目标死亡，另外复查惊惧斩、上下烟尘和普通玩法。

改动前备份：`C:/Users/yummn/Downloads/kait-backups/ice-a-before-20260905-123840`。本任务仅更新 Windows，不发起 Android 构建。

2026-09-05 验证：EditMode 共 382 项通过，0 失败，报告 `Logs/ice-a-tests.xml`。Windows 构建成功，日志 `Logs/ice-a-build.log`。已查看保持与碎裂阶段的实际角色截图，冰晶贴在脚边，面部和血条保持可读。截图在 `VFXScreenshots/ice-a-*.png`，属于诊断场景，不是正常开局。

Windows 运行检查共 9 项全部通过：冻结保持、移动跟随、解除、死亡清理、惊惧斩、上下急停、普通命中和 12 步玩法。
