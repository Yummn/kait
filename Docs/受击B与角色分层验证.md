# 受击 B 与角色分层

2026-09-04

本次采用受击预览的 B 方案：白色主体、灰紫色边缘的小型中空闪光。素材直接使用已选图片，没有再次绘制或改色。八帧播放约 0.26 秒，保留原来短暂的人物闪白；实际扣血才触发，不改伤害、音效和操作时序。

## 显示顺序

从下到上：敌人 → 敌人侧命中反馈 → Kait。命中、击杀、连杀、格挡、推动均进入中间层；Kait 自身的 B 受击效果在 Kait 上方。

站立和移动中的 Kait、残影、敌人移动体及脱离逻辑的死亡体分别放入各自的父层。角色内部重新排序不会越过这三个父层。没有新增人物遮罩。

## 检查记录

- Unity EditMode：333 项全部通过，记录在 `Logs/hurt-b-tests.xml`。覆盖八帧取样、贴图导入、特效分组、角色重排后的层级关系。
- Windows Player：受击、普通命中、重叠命中、击杀、连杀、格挡、推动及 12 步移动检查均正常退出并生成截图，无检查项中的空引用、数组越界或层级断言错误。
- `hit-overlap` 是刻意让 Kait 与敌人重叠的诊断场景，不是正常站位。截图用于确认特效盖住敌人，但被 Kait 遮挡。
- B 的首次定时抓拍错过了亮帧，改为在诊断模式同步取动画第 2 帧。该截图用于检查图形、尺寸和层级，不代表逐帧性能测量；正常游戏仍按真实时间播放八帧。
- 接入前的三份相关脚本备份：`C:/Users/yummn/Downloads/kait-backups/hurt-b-layers-before-20260904-230235`。这是本次代码备份，不是整个项目的完整备份。

运行检查脚本：`Tools/verify_hurt_b_runtime.ps1`。截图目录：`VFXScreenshots/hurt-b-*.png`。本次只更新 Windows 版，不生成 Android 包。
