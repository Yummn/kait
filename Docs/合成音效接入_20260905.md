# 合成音效更新

本次选择 Merge_B，移动起步继续使用原有拔剑音效，不接入 DrawSword 候选。

合成音效位于 `Assets/Resources/Audio/UI/SelectedModel/Merge_B.wav`，来自 `kait-sfx-model-audition-20260904/board_draw_round1/Merge_B.wav`，时长 0.82 秒，48 kHz 双声道。文件 SHA256 为 `304D84FDB4F2F14733749B6B573D2D95941748F1CD97099828BB406440FBC12C`，复制后完全一致。

只修改 GameAudio 的 MergePath。保留原有合成点数升调及播放音量，不调整音频文件的音量、淡出或峰值；旧的 Audio/Merge 也保留，方便回退。

修改前的 GameAudio 和项目设置保存在 `Backups/merge-b-before-20260905`。本次仅更新 Windows 版，不编译安卓。

验证结果：EditMode 测试 380/380 通过，报告 `Logs/merge-b-tests.xml`；Windows 打包成功，日志 `Logs/merge-b-build.log`，Unity 返回码 0。启动入口为 `Build/kait.exe`，需配套整个 Build 文件夹使用。
