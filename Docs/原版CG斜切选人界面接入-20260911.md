# 原版 CG 斜切选人界面

2026-09-11，按用户确认的网页预览接入 Unity 原生 UI。

## 已改

- 真琴 104331 对应 Kait、万圣节宫子 108231 对应 Yummn，直接使用上一轮获批的两张原版 CG。正式图片与预览原文件 SHA256 一致，来源保留在 Tools/CharacterSelectPreview/README.md。
- 左右角色共用一条斜切边界；大图等比取景，不拉伸。下方渐暗保证说明可读，大字角色名与三项特点保留。
- 点击画面切换焦点；点击“选择”直接开始对应角色。继续入口仍读取原来的独立角色存档，包含 Yummn 的 v0.8.2、v0.8.1 和 v0.8 存档；无存档不展示入口。
- 返回仍回主菜单。选人页独立于战场风格裁切，不创建 Spine 预览角色，不影响战场人物遮罩和动画。
- 页面以 1920×1080 等比适配，额外宽高由底色填充；不把界面拉伸到不同屏幕比例。

## 文件

- Assets/Scripts/KaitCharacterSelection.cs：原生布局、裁切网格、按键及焦点。
- Assets/Scripts/KaitGame.Yummn.cs：接回原开始、继续和返回逻辑。
- Assets/Resources/KaitVisuals/CharacterSelection：两张正式 CG。
- Assets/Editor/KaitCharacterCGImport.cs：保留尺寸、关闭压缩与 mipmap、双线性过滤。
- Assets/Scripts/KaitGame.CharacterCGQA.cs、Tools/verify_character_cg_runtime.ps1：独立运行检查与玩家偏好还原。

## 验证

- character-cg-tests-final.xml：3/3 通过，1920×1080、2400×1080、1280×720 的布局与按钮回调；不是全量规则测试。
- 首轮发现大字体行高超出容器，已调整角色名与装饰序号的容器高度。
- Windows 首轮构建与运行完成：两位角色开始、返回、读取各自新建存档；37 项玩家偏好按原类型和值还原。
- 实际截图检查后发现线性颜色空间使渐暗偏浅，已补偿渐暗顶点颜色。最终 character-cg-final-build.log Success、退出0，程序集14:22:32。再次运行 CHARACTER_CG_QA_COMPLETE、37项偏好还原，并查看最终 character-cg.with-saves.png。

不修改战斗规则，不构建 Android，不上传 GitHub。网页预览仍保留作为选稿记录，不是正式游戏依赖。
