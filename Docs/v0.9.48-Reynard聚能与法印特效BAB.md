# v0.9.48 Reynard 聚能与法印特效 BAB

## 接入内容

- 聚能弹采用 B「双环增幅」。贴身法球实际造成额外伤害时，在命中格播放双环收束与回弹。
- 符文引爆采用 A「月印碎裂」。消耗法印并实际找到映射格目标时，播放月印破碎。
- 法阵共振采用 B「星桥脉冲」。相连法印实际命中目标时，在对应格播放星桥传播反馈。
- 正式资源为 `ChargedOrb.png`、`RuneDetonation.png`、`CircleResonance.png`，继续沿用统一的月蓝、淡紫、冰青主色。

## 规则边界

- 不修改三张牌的伤害、范围、支付顺序或触发次数。
- 新视觉事件只在原规则确实造成伤害的位置生成，不额外结算伤害。
- 应用版本升至 0.9.48，Android 版本码预置为 948；本轮不生成 Android、不上传 GitHub。

## 验证

- 三张正式资源均为透明 4×2 八帧。
- `Logs/reynard-vfx-bab-tests.xml`：Reynard 专项 19/19 通过。
- `Logs/reynard-vfx-bab-build.log`：Windows 构建成功，`Build/kait.exe` 与配套数据已同步为 v0.9.48。
- `Logs/reynard-runtime.log`：完成 `HOME0926_SELECTION_QA_COMPLETE` 与 `REYNARD_QA_COMPLETE`。
- 已查看 `Build/Logs/reynard-charged-orb.png`、`reynard-rune-detonation.png`、`reynard-circle-resonance.png`，三组特效位置、透明边缘、尺寸及遮挡关系正常。
