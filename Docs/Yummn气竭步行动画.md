# Yummn 气竭步行

2026-09-10

- 气竭移动改为 `01_walk`，正常 1 倍速循环。
- 气竭移动不生成残影，同时清除上一段移动尚未消退的残影。
- 高速移动仍使用 `01_run`，保留原有残影数量、颜色和移动曲线。
- 使用行动开始时的状态决定表现。回满 5 气的那一步仍为步行且无残影；耗尽最后一点气的高速行动仍使用奔跑表现。
- 击杀后的跟进仍保留 `01_standBy`，不会被步行或奔跑截断；传送的原有特效不变。
- 不修改规则、伤害、耗气或敌人动画。

测试：671 项 EditMode 检查通过（`Logs/yummn-walk-tests.xml`）。本次同步 Windows，不重新打包 Android。

Windows 实际运行检查通过：连续五次气竭步行均播放 `01_walk`、残影对象数为零，包含回满气的最后一步；原有高速四方向移动、首击追击、击杀和疾风连击检查也通过。日志：`Logs/yummn-walk-runtime.log`（`YUMMN_WALK_QA_COMPLETE`）。测试后已恢复原有存档和设置。
