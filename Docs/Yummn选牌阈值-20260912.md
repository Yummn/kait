# Yummn选牌阈值

新局默认8+8合成16时获得一组三选一。Yummn独立设置新增“技能获得阈值：32（关闭为16）”，开启后只在16+16合成32时发牌。不是两个门槛同时奖励，也不改变敌人或Boss生成值。Kait仍为32。

设置下一局生效，存入本局规则快照。旧存档缺少该字段时继续使用32，避免回放的奖励顺序改变；16与32的成绩规则键分开。漫画字幕和详细说明跟随实际规则。

Logs/reward16-final-tests.xml：6/6通过，包括实际合成里程碑入口、两种门槛、快照往返、旧字段兼容、教程及Kait不变。Windows构建结果见共享修改清单；本轮不打Android、不上传GitHub，未运行实机视觉验收。
