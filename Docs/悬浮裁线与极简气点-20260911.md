# 悬浮裁线与极简气点

2026-09-11

## 改动

- 卡通侧保留已选B气流水滴，极简侧换成青蓝实心圆点；空槽为暗色圆点，气竭降低饱和度，部分气量按同一数值填充。
- 原背景裁线基于全屏，而UI基于放大1.2倍的Game Content，斜率不一致，导致不同高度看起来偏移不同。
- GlobalStyleSplit现在支持以源坐标为单位的固定横向偏移。UI统一基于Background，偏移-12设计像素；面板、按钮、卡牌、文字和图标共用此上下文，不重复叠加偏移。
- 屏幕危险预警使用独立零偏移上下文，仍贴合背景裁线。

## 验证和交付

- 相关EditMode测试9/9通过：气点状态3项、不同高度/尺寸/嵌套裁剪3项、危险边缘3项。未重复跑全量。
- Logs/float-split-build.log：Success、正常退出；Build/kait_Data/Managed/Assembly-CSharp.dll时间13:18:11。
- Tools/verify_float_split_runtime.ps1运行通过，Logs/float-split-runtime.log有YUMMN082_QA_COMPLETE；37项玩家设置和存档按原类型和值还原。
- 已查看Logs/float-split.six-ki.png、Logs/float-split.partial-recovery.png实际游戏截图，确认左右样式和上下裁线。卡牌拖动未单独手动验收，沿用同一裁剪上下文。
- Windows已同步；Android和GitHub未更新。
