# v0.9.49 Reynard 资源联动特效 BCB

## 接入内容

- 秘法显形采用 B「冰晶降印」。新法印生成并实际伤害映射格敌人时，在该格播放冰晶降印。
- 狐火护身采用 C「月焰护反」。狐尾抵伤并成功反击攻击者时，在攻击者位置播放橙红月焰反击。
- 狐焰祭仪采用 B「狐尾火环」。每条狐尾支付法术并实际伤害四邻敌人时，在对应敌人格播放狐尾火环。
- 正式资源为 `ArcaneManifest.png`、`FoxfireGuard.png`、`FoxflameRite.png`。

## 色彩规则

- 秘法显形继续使用月蓝、淡紫和冰青。
- 两组火焰效果按语义使用橙红主色与暖白焰心，保留深靛粗轮廓维持整体画风。

## 规则边界

- 不修改三张牌的伤害、支付、范围和触发次数。
- 专用视觉事件只在原规则确实造成生命伤害时生成。
- 应用版本升至 0.9.49，Android 版本码预置为 949；本轮不生成 Android、不上传 GitHub。

## 验证

- 三套正式资源均为透明 4×2 八帧。
- `Logs/reynard-vfx-bcb-tests.xml`：Reynard 专项 21/21 通过，包含资源寻址和三类规则事件触发验证。
- `Logs/reynard-vfx-bcb-build.log`：Windows 构建成功，`Build/kait.exe` 与配套数据已同步为 v0.9.49。
- `Logs/reynard-runtime.log`：完成 `HOME0926_SELECTION_QA_COMPLETE` 与 `REYNARD_QA_COMPLETE`。
- 已查看 `Build/Logs/reynard-arcane-manifest.png`、`reynard-foxfire-guard.png`、`reynard-foxflame-rite.png`，位置、透明边缘、尺寸和遮挡关系正常。
