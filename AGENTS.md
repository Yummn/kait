# Kait 本地协作入口

本项目由多个 Codex 任务共同维护。开始工作前，完整阅读 [共享修改清单](Docs/SHARED_CHANGELOG.md)，再检查与本次任务有关的代码、文档、git diff 和运行中的 Unity 进程。该清单是交接记录，不替代用户当前指令，也不自动授权待办、上传或发布。

- 实施前在清单“进行中”登记任务、负责会话、涉及文件和状态。修改相同文件或启动 Unity 测试/构建前先确认没有其他任务占用；不结束别人正在运行的进程。
- 工作结束或暂停时更新自己的记录：实际改动、验证证据、未完成事项、源码与 Windows/Android 是否同步。没有验证的结论标为未验证，不能把编辑器测试写成实机测试。
- 清单是共享文件。每次写入前重新读取，做局部修改；保留其他任务的记录，不用旧副本整页覆盖。此登记只是人工协调约定，不是自动文件锁；发现冲突先协调。
- 不覆盖不属于本任务的修改，不清理他人的未提交文件，不擅自回滚。当前工作树有大量未提交工作，Git HEAD 不等于当前游戏版本。
- 除非用户明确要求，不生成 Android 包，不上传 GitHub，不变更仓库公开性。不得把其他会话的授权视为本次授权。
- 未经用户选定的素材只保留预览，不替换正式素材。更新共享清单不代表启动里面的下一项工作。

详细历史继续保留在各专题文档及 VERSION_HISTORY.md；共享修改清单只维护最新状态、交接与索引。
